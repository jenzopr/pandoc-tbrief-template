\documentclass[$language$]{tfbrief}
\usepackage{babel}
\usepackage{eurosym}
\usepackage{hyperref}

% Handle the from dict
$if(from)$
\fromname{$from.name$}

$if(from.street)$
\fromstreet{$from.street$}
$endif$
$if(from.city)$
\fromcity{$from.city$}
$endif$
$if(from.city-code)$
\fromcitycode{$from.city-code$}
$endif$
$if(from.country)$
\fromcountry{$from.country$}
$endif$
$if(from.country-code)$
\fromcitycc{$from.country-code$}
$endif$
$if(from.email)$
\fromemail{$from.email$}
$endif$
$if(from.www)$
\fromwww{$from.www$}
$endif$
$if(from.phone)$
\fromphone{$from.phone$}
$endif$
$if(from.fax)$
\fromfax{$from.fax$}
$endif$
$if(from.mobile)$
\frommobile{$from.mobile$}
$endif$
$endif$

% set the width of the "from" column by using the width of
% a given text should only be neccessary, if it cannot be
% automatically detected (e.g. due to multiline \fromstreet)
%\settowidth{\fromwidth}{Platz der deutschen Einheit 5a}

% Handle the to dict
$if(to)$

$if(to.name)$
\toname{$to.name$}
$endif$
$if(to.street)$
\tostreet{$to.street$}
$endif$
$if(to.city)$
\tocity{$to.city$}
$endif$
$if(to.city-code)$
\tocitycode{$to.city-code$}
$endif$
$if(to.country-code)$
\tocitycc{$to.country-code$}
$endif$
$if(to.country)$
%\tocountry{$to.country$}
$endif$
$if(to.fax)$
\tofax{$to.fax$}
$endif$
$endif$

$if(yourref)$
\yourref{Natalie Heuer}
$endif$
$if(myref)$
\ourref{$myref$}
$endif$

% Handle the date
$if(date)$
\date{$date$}
$else$
\date{\today}
$endif$

\subject{$subject$}

$if(greeting)$
\greeting{$greeting$}
$endif$

$if(signature)$
\usepackage{graphics}
\signature{\includegraphics{$signature$}\\}
$endif$

$if(leave)$
\leave{$leave$}
$endif$

$if(logo)$
\usepackage{graphics}
\logo{\includegraphics{$logo$}}
$endif$

\begin{document}
\begin{letter}

$body$

\end{letter}

% if you like to have the attachment closer to the page bottom,
% enlarge the current page
%\enlargethispage{1cm}
\vspace*{1cm}

%Attachments
$if(attachment)$
\begin{attachment}
$for(attachment)$
\item $attachment$
$endfor$
\end{attachment}
$endif$

\end{document}
